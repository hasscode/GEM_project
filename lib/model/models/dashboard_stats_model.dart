class DashboardStats {
  final int total;
  final int verified;
  final int denied;
  final int remaining;

  DashboardStats({
    required this.total,
    required this.verified,
    required this.denied,
    required this.remaining,
  });
}
